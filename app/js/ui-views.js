/* Mittagio UI-Views – Render-Funktionen [Big Split]
 * Enthält: renderDiscover, renderDiscoverCategories, renderDiscoverDays, renderChips,
 * createModernOfferCard, createDiscoverListCard, renderProviderHome, renderProviderWeekPreviewNew,
 * renderProviderPickups, renderCookbook, renderWeekPlan, renderCart, renderCheckout,
 * renderFavorites, renderOrders, openOffer.
 * Globals via window: offers, provider, cookbook, week, activeDay, activeDiscoverFilter, ...
 */
(function(){
  'use strict';
  /* UI-Views wird schrittweise migriert.
   * Aktuell: Alle Funktionen bleiben in script.js.
   * Nach vollständiger Extraktion: Hier die IIFE mit window-Referenzen und Funktionskörper.
   */
  /* Render-Funktionen bleiben in script.js; hier Platzhalter für spätere Extraktion. */
})();
