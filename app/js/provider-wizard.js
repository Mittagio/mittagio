/* Mittagio Provider-Wizard – Inseratsflow [Big Split]
 * Enthält: closeWizard, openDishFlow, startListingFlow, buildListingStep, publishOffer,
 * showPublishFeeModal, closePublishFeeModal, showInseratSuccessSheet, closeInseratSuccessSheet,
 * saveToCookbookFromWizard, previewOfferFromWizard, etc.
 * Globals via window: w, provider, offers, cookbook, week, LS, load, save, providerId, isoDate,
 * normalizeOffer, normalizeProviderProfile, buildAddress, DEFAULT_MEAL_WINDOW, cryptoId, offerCard, esc, euro, ...
 */
(function(){
  'use strict';
  /* Provider-Wizard wird schrittweise migriert.
   * Aktuell: Alle Funktionen bleiben in script.js.
   * Nach vollständiger Extraktion: Hier die IIFE mit window-Referenzen und Funktionskörper.
   * Siehe script.js Zeilen 6364-6390, 6587-6676, 14948-17057.
   */
  /* Wizard-Funktionen bleiben in script.js; hier Platzhalter für spätere Extraktion. */
})();
