---
description: Inseratsflow "Slim-Flow" (Express) – Single-Page-Listing, Profil-Automatisierung, Button-Hierarchie, Planer-Integration.
alwaysApply: false
---

# Slim-Flow: Inseratsflow radikal verschmälern

## Ziel

- **Eine Hauptseite** für die Eingabe: Gerichtsname (Autocomplete), Bild (3 Slots: Kamera, Galerie, KI), Preis (groß, `inputmode="decimal"`).
- **Profil-Automatisierung**: Abholzeiten, Vor Ort, Mehrweg, Allergene-Standard aus Profil – keine extra Screens, keine Abfrage wenn im Profil hinterlegt.
- **Visuelle Hierarchie**: Nach Preiseingabe Fokus auf CTA. Icons (🍴, 🧾, 🔄) nur unter dem Bild in der Vorschau; Begriff „Abholnummer“ (nicht „Abhol-Nr.“).
- **Checkout**: Primärer Button „Jetzt für 4,99 € inserieren“. Erst nach Klick Upsell-Overlay für Abholnummer (0,89 €) mit emotionalem Übergabe-Foto.

## Button-Hierarchie (Planer-Integration)

- **Hero-Button (sticky unten)**: „Jetzt für 4,99 € inserieren“ – Emerald-Grün (#10b981), volle Breite, dominierend.
- **Planer-Zeile direkt darunter**: Zwei dezentere Outline-Buttons / Text-Links mit Icons:
  - **📅 Planen**: Öffnet kompaktes Day-Picker-Overlay (Mo–So). Nach Wahl: Toast „Für [Wochentag] geplant!“, Overlay schließt.
  - **📖 Speichern**: Gericht ins Kochbuch speichern (ohne sofort zu inserieren).
- Design: Planer-Buttons mit Slate-Border oder dezentem Grau, damit der Emerald-CTA dominiert.

## Technik

- **Wizard** (`buildListingStep`): Schritt 0 = Einseiter mit Name, Bild, Preis. Abholzeit/Kategorie aus Profil oder Autocomplete im Hintergrund setzen. Optional: Schritt „Allergene & Service“ überspringen wenn Profil-Standards gesetzt („Mit Standardeinstellungen fortfahren“ ausbauen oder vorbelegen).
- **Preis-Input**: `inputmode="decimal"`, Platzhalter z.B. „8,50“, **massiv groß** (min-height, font-size) für Küchenumgebung.
- **Day-Picker**: Kompaktes Overlay (Mo–So), ein Tap = Tag gewählt, dann Eintrag in `week` für gewählten Tag, Toast, Overlay schließen.

## Referenz

- Bestehender Wizard: `startListingFlow` → `startWizard('listing')` → `buildListingStep()`. Schritt 0 enthält bereits Name, 3-Slot-Bild, Abholzeit, Preis, Kategorie. Slim = Abholzeit/Kategorie aus Profil verstecken, Preis vergrößern, CTA + Planer-Zeile am Ende von Schritt 0.
