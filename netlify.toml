[build]
  command = "npm run build"
  publish = "dist-react"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20"

# /app/* → App-SPA (eigene index.html); danach Root-SPA (Landing)
[[redirects]]
  from = "/app/*"
  to = "/app/index.html"
  status = 200
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
